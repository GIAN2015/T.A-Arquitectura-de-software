# Frontend

Aquí está separada la parte visual del sistema.

Incluye:

- `templates/`: vistas HTML
- `static/`: CSS, JS e imágenes fuente
- `staticfiles/`: archivos recolectados por Django

El backend Django consume estas rutas desde `backend/config/settings/base.py`.
