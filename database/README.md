# Database

Aquí se agrupa la persistencia y soporte de base de datos.

Incluye:

- `db.sqlite3`: base local de desarrollo
- `docker-compose.yml`: apoyo para base de datos
- `docker-compose.prod.yml`: variante productiva

Si luego migras a PostgreSQL, este bloque ya queda listo para crecer.
