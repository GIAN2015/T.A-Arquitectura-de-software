# Backend

Aquí vive el backend Django del sistema.

Incluye:

- `manage.py`
- `requirements.txt`
- `config/`
- `apps/`
- scripts de ejecución
- entorno virtual local

Ejecuta desde aquí:

```bash
source venv/bin/activate
python manage.py runserver
```
